# This folder moved

The Word builds now live with their track, because `education/` holds more than one study track:

**➡️ [`education/k8s-k3s-redpanda/docx/`](../k8s-k3s-redpanda/docx/)** — the seven chapters on
Kubernetes (k3s), Redpanda and the order management system built on them.

Start at [`education/README.md`](../README.md) for the full list of tracks, or go straight to the
[track 1 chapter list](../k8s-k3s-redpanda/README.md).

*This file exists only so that previously shared links to `education/docx` still resolve.*
